<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.playchoice.mybatis.mappers.Login">
	<select id="login" resultType="memberDTO">
	SELECT *
	FROM member
	WHERE 
		m_id = #{m_id}
		AND m_pw = #{m_pw}
	</select>
	
	<select id="findId" resultType="memberDTO">
	SELECT *
	FROM member
	WHERE 
		m_name = #{m_name}
		AND m_mail = #{m_mail}
	</select>
	
	<update id="findPw">
	UPDATE member
	SET
		m_pw = #{m_pw}
	WHERE
		m_id = #{m_id}
		AND m_mail = #{m_mail}
	</update>
</mapper>