<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <!-- com.playchoice.play.dto.PlayDTO --> 
<mapper namespace="com.playchoice.mappers.adminplayMapper">
									<!-- com.playchoice.play.dto.PlayDTO -->
    <select id="listAll" resultType="PlayDTO">
    	select * from play
    </select>
    
    <select id="read" resultType="PlayDTO">
    	select p_id, p_name, p_info, p_refund_policy, p_location, p_status, p_picture
    	from play
    	where p_id = #{p_id}
    </select>
    
    <insert id="create" parameterType="java.util.HashMap">
    	insert into play(p_name, p_info, p_picture)
    	values(#{p_name}, #{p_info}, #{p_picture})
    </insert>
    
    <update id="update" parameterType="java.util.HashMap">
    	update play set p_name=#{p_name}, p_info=#{p_info}, p_picture=#{p_picture}
    	where p_id=#{p_id}
    </update>
    
    <update id="delete">
    	update play set p_status=0
    	where p_id=#{p_id}
    </update>
    
    <update id="flurry">
    	update play set p_status=1
    	where p_id=#{p_id}
    </update>
</mapper>